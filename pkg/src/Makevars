PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript" -e "RcppArmadillo:::CxxFlags()")
PKG_LIBS += $(SHLIB_OPENMP_CXXFLAGS)
PKG_CPPFLAGS += $(SHLIB_OPENMP_CXXFLAGS)
